import { Routes } from '@angular/router';
import { LoginComponent } from '../../login/login.component';

export const ADMIN_CONTENT_ROUTES: Routes = [
  {
    path: 'login',
    component: LoginComponent
  }
];
